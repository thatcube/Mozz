# macOS
.DS_Store

# Xcode / SwiftPM
.build/
build/
DerivedData/
*.xcodeproj/xcuserdata/
*.xcworkspace/xcuserdata/
xcuserdata/
*.xcuserstate
Package.resolved

# Generated Xcode project (regenerated by `xcodegen generate` / tools/generate-project.sh)
Mozz.xcodeproj/
# Info.plist is generated from project.yml's `info:` block by XcodeGen
App/Mozz/Info.plist
# Entitlements are generated from project.yml's `entitlements:` block by XcodeGen
App/Mozz/Mozz.entitlements
App/MozzWidget/MozzWidget.entitlements
App/MozzWidget/Info.plist

# SwiftPM local config
.swiftpm/

# fastlane / secrets (never commit)
.env.fastlane
*.p8
# fastlane run artifacts
fastlane/report.xml
fastlane/Preview.html
fastlane/test_output/
WHAT_TO_TEST.txt

# Editor
*.swp

# AI/agent working docs — kept local, never committed (keeps `main` human-authored)
AGENTS.local.md
CLAUDE.local.md
.ai/
.agents/
*.agent.md
.project.yml.verbak

# Screenshot fixture: large binary covers + audio, generated locally.
ScreenshotAssets/
build/
